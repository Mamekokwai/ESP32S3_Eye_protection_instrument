# 代码移植指南

## 目录

- [1. 移植原则](#1-移植原则)
- [2. 代码模块关系](#2-代码模块关系)
- [3. 建议的移植顺序](#3-建议的移植顺序)
  - [3.1 建立平台配置](#31-建立平台配置)
  - [3.2 移植显示接口](#32-移植显示接口)
  - [3.3 移植文件和媒体接口](#33-移植文件和媒体接口)
  - [3.4 移植音频接口](#34-移植音频接口)
  - [3.5 移植命令输入](#35-移植命令输入)
- [UART 代码移植](#uart-代码移植)
  - [接收信息处理](#接收信息处理)
  - [指令解析与执行](#指令解析与执行)
  - [指令回显与响应](#指令回显与响应)
  - [并发、超时和错误处理](#并发超时和错误处理)
- [4. 必须保留的生命周期规则](#4-必须保留的生命周期规则)
- [5. 内存和并发适配](#5-内存和并发适配)
- [6. 编码和路径适配](#6-编码和路径适配)
- [7. 编译集成清单](#7-编译集成清单)
- [8. 功能验证](#8-功能验证)
- [9. 移植交付内容](#9-移植交付内容)

本文只说明如何把本项目中的软件模块移植到另一套工程或另一种运行平台。移植范围是代码结构、接口和依赖替换，不规定芯片型号、开发板、引脚、Flash 容量、文件系统介质或量产安全配置。目标平台的硬件参数应由目标工程单独定义。

## 1. 移植原则

- 保留上层业务接口，优先替换底层适配层，不要在播放器和命令解析器中直接写平台寄存器。
- 把平台相关内容集中到配置头文件、BSP 或适配函数中，避免业务代码散落条件编译。
- 明确每个模块的所有权：谁创建资源，谁停止任务，谁释放缓冲区；重复启动和异常退出必须安全。
- 保持错误码语义稳定。底层错误转换到统一的 `esp_err_t`/项目错误码后再向上返回。
- 移植完成后先验证单模块，再验证模块组合，最后验证长时间运行和异常恢复。

## 2. 代码模块关系

当前代码可以按以下层次拆分：

```text
业务状态机 / app_main
├─ UART/USB 命令解析
├─ Flash 媒体索引与 Flash 播放器
├─ SD 媒体索引与 SD 播放器
├─ 图片查看器
└─ 音频服务与播放器

BSP/平台适配层
├─ LCD 输出
├─ SD/FATFS 文件访问
├─ I2C/I2S 音频控制
├─ DMA/缓存/内存分配
└─ 定时器、任务和串口收发
```

上层只依赖 `.h` 文件声明的接口。移植时应先列出目标平台对应的 LCD、存储、音频、串口、定时器和内存实现，再逐项替换 BSP。

## 3. 建议的移植顺序

### 3.1 建立平台配置

新建一个目标平台配置（例如 `port_config.h`），集中定义：

- 显示宽高、像素格式、刷新方向和屏幕数量；
- 文件系统根路径、路径编码和目录遍历能力；
- 音频采样率、位宽、声道数和输出设备；
- 命令输入通道及其发送/接收回调；
- 定时器分辨率、任务优先级和栈大小；
- 可用于 DMA 的内存类型及缓存对齐要求。

业务模块不应直接引用目标板的 GPIO、寄存器或驱动句柄。

### 3.2 移植显示接口

为 LCD 适配层实现初始化、复位、区域写入、刷新完成等待和背光控制等操作。播放器只应提交 RGB565（或适配层声明的像素格式）图像条带，不应关心总线类型。

必须明确：

- `write_region` 是否异步；
- DMA 提交后源缓冲区何时可以复用；
- 刷新完成回调在哪个任务/中断上下文执行；
- 超时、取消和设备断开时如何返回错误。

如果目标显示驱动是异步 DMA，移植时要保留“提交—等待—释放”生命周期，否则重复播放可能出现黑屏、花屏或内存错误。

### 3.3 移植文件和媒体接口

媒体索引和播放器通过文件抽象读取数据。目标平台至少需要实现：打开、定位、读取、获取长度、关闭，以及目录递归（如果需要 `VIDLIST/IMGLIST/ALIST`）。

不要在 AVI/JPEG/MP3 解析器内写死磁盘路径或平台文件句柄；将路径编码转换、大小写规则和目录分隔符放到文件适配层。

### 3.4 移植音频接口

音频代码分为 codec 控制、数据输出和播放器服务三部分。目标平台可以替换 codec，但应保留以下上层能力：初始化/反初始化、设置音量、静音/取消静音、提交 PCM 数据、停止和查询状态。

I2C、I2S、DMA 和功放控制都属于底层适配内容。上层播放器只提交采样数据，并处理“设备未就绪、缓冲不足、停止中、无内存”等状态。

### 3.5 移植命令输入

命令解析器应接收完整的一行文本，再执行统一的命令分发函数。串口、USB、网络或测试脚本都实现相同的输入/输出回调即可。

建议保持以下抽象：

```c
typedef struct {
    int  (*read_line)(char *buf, size_t capacity, uint32_t timeout_ms);
    int  (*write_text)(const char *text, size_t length);
    void *context;
} command_channel_t;
```

命令解析器不应依赖某个串口驱动、终端颜色或操作系统控制台。编码转换也应放在通道层，解析器内部统一使用 ASCII 命令和 UTF-8/项目内部字符串约定。

## UART 代码移植

本节描述本项目 UART 命令链路的代码级移植方法。UART 只是传输介质，接收、解析、执行和响应应拆成独立层；换成 USB、网络或其他串行接口时，只替换通道适配层即可复用同一套命令处理代码。

### 接收信息处理

通道适配层负责从底层读取字节并组装成完整命令行，建议流程如下：

1. 创建固定大小的接收缓冲区，并维护当前写入位置；
2. 逐字节接收数据，过滤或记录 `\\r`，以 `\\n`（或兼容 `\\r\\n`）作为一行结束；
3. 检查缓冲区剩余空间。超长行应丢弃到下一个换行符，并返回明确的长度错误；
4. 在行尾补 `\\0`，去除首尾空白后再交给解析器；
5. 接收任务只负责收集数据和投递消息，不在驱动回调、中断或高优先级接收路径中执行文件、解码、显示等耗时操作。

推荐把每条消息封装为“来源通道 + 文本 + 长度”，这样多个 UART/USB 通道可以同时接收，而不会共用可变的全局输入缓冲区：

```c
typedef struct {
    command_channel_t *channel;
    char              *line;
    size_t             length;
} command_message_t;
```

如果底层驱动会因暂时无数据返回超时，不要把超时当成命令结束；只有收到换行符才提交一条命令。通道关闭时应投递一个关闭事件，让接收任务能够退出并释放缓冲区。

### 指令解析与执行

解析器接收一行文本后，按“命令名 + 参数”拆分，命令名建议不区分大小写，参数按协议要求校验。解析阶段只做语法和范围检查，不直接操作底层外设。

指令执行建议采用静态分发表或等价的注册表：

```c
typedef int (*command_handler_t)(const command_message_t *message,
                                 int argc, char **argv);

typedef struct {
    const char          *name;
    command_handler_t    handler;
} command_entry_t;
```

执行链路应保持以下顺序：

```text
收到字节 → 组装整行 → 清理空白 → 拆分参数 → 查找命令
        → 校验参数 → 调用业务 API → 生成结果 → 写回同一通道
```

例如 `VID 1` 只负责把序号解析为整数并调用视频业务 API；播放器的停止、资源释放和错误转换由播放器模块完成。未知命令、参数数量错误、数值越界和业务状态不允许时，都必须走统一错误响应路径。

长耗时指令（媒体扫描、图片解码、音频播放初始化等）不应阻塞接收任务。可以将已校验的命令投递到业务队列，由业务任务执行；接收任务应继续服务其他通道。若协议要求严格按顺序执行，则只在业务调度层串行化，不要锁住底层串口。

### 指令回显与响应

“回显”应区分为输入回显和执行响应：

- 输入回显：可选地原样返回收到的命令行，用于调试终端确认链路；不应把回显当作执行成功。
- 执行响应：命令完成后返回 `OK ...` 或 `ERR ...`，其中应包含命令名和必要的结果数据。
- 列表类命令可以先返回命令确认，再逐行输出结果，最后输出结束标记（如果上层协议需要）。
- 所有响应都必须通过产生该命令的 `message->channel` 发回，不能写到“当前默认串口”，否则多通道同时工作时会串线。

建议统一响应函数，并集中处理格式化、编码和发送失败：

```c
int command_reply_ok(const command_message_t *message,
                     const char *fmt, ...);
int command_reply_error(const command_message_t *message,
                        int error_code, const char *fmt, ...);
```

响应文本的编码由通道适配层决定。命令名和错误码保持 ASCII；文件名等业务数据在输出前转换为该通道配置的编码。USB、UART 或测试虚拟通道若需要前缀，应在通道层添加，不要修改业务层的响应字符串。

### 并发、超时和错误处理

- 每条命令保存自己的来源通道和缓冲区，禁止多个通道共享同一静态响应缓存。
- 写响应时设置超时；底层发送阻塞或断开时，记录错误并释放本条消息，不能让业务任务永久等待。
- 通道发送失败不应回滚已经完成的业务动作；下一条命令仍应可以执行，并可通过 `STATUS` 查询真实状态。
- 停止、复位或通道关闭时，先停止接收和业务分发，再等待正在执行的命令退出，最后销毁队列和通道资源。
- 为接收缓冲、消息队列和响应缓存设置上限，并统计溢出、丢包、超时和未知命令次数，便于移植后的现场诊断。
- 如果需要跨任务返回异步结果，应定义请求 ID 或事件通知格式，避免把后续命令的响应误认为前一条命令的回显。

## 4. 必须保留的生命周期规则

- 播放器启动前先停止并等待旧实例退出。
- 解码任务退出后，才能释放其使用的帧缓冲、条带缓冲和文件句柄。
- 异步显示提交完成前，不得复用或释放 DMA 源缓冲区。
- 音频停止要先停止数据生产，再停止输出设备，最后释放解码器和文件资源。
- 初始化失败必须按已成功创建的资源逆序清理，可重复调用反初始化。
- 所有跨任务队列、信号量和回调都要定义关闭状态，避免任务永久等待已销毁的对象。

## 5. 内存和并发适配

移植到不同平台时，重点检查以下约束：

- JPEG 解码缓冲、视频帧缓冲和 DMA 缓冲是否位于正确的内存区域；
- DMA 缓冲的地址对齐、大小对齐和缓存一致性要求；
- 大块缓冲失败时是否返回可诊断错误，而不是继续使用空指针；
- 播放器任务、命令任务和音频任务之间是否存在锁顺序反转；
- 定时器回调是否只发送事件，不执行耗时的文件、解码或显示操作；
- 目标系统的 tick、时间戳和超时单位是否与现有代码一致。

若目标平台没有独立的高速内存，应降低条带高度或缓冲数量，并重新测量吞吐、延迟和撕裂情况；不要仅通过增大普通堆来掩盖 DMA 内存不足。

## 6. 编码和路径适配

文件名、命令响应和日志可能使用不同编码。移植时应明确三种边界：

1. 文件系统返回的原始文件名字节；
2. 媒体索引内部保存的统一字符串格式；
3. 各命令通道输出时使用的编码。

编码转换应集中实现并检查缓冲区长度、非法字节和无法映射字符。不要在每个命令处理函数中重复转换，也不要用固定长度拷贝替代长度检查。

## 7. 编译集成清单

- 将需要的 `.c/.h` 加入目标工程的组件构建文件。
- 检查 include 路径、组件依赖和编译宏，删除只属于原平台的宏定义。
- 为新平台提供缺失的系统调用、时间函数、互斥量、队列和日志接口。
- 将平台相关默认值移到配置文件，不要在公共头文件中定义冲突的全局宏。
- 开启编译器警告，修复指针类型、整数宽度、格式化字符串和未使用返回值问题。
- 确认 C/C++ 链接边界使用 `extern "C"`（如果目标工程包含 C++）。
- 先做全量编译，再做目标平台的最小功能构建，避免只编译到未使用的适配代码。

## 8. 功能验证

建议按以下顺序验证：

1. 平台层自测：显示写入、文件读写、音频输出、通道收发、定时器和内存分配。
2. 单模块测试：JPEG 解码、AVI 索引、MP3 解码、目录遍历、命令解析。
3. 组合测试：图片切换、视频停止后切换图片、视频和音频并发、重复启动/停止。
4. 异常测试：文件不存在、格式错误、设备断开、读写超时、分配失败、通道关闭。
5. 长稳测试：连续播放、反复切换媒体、持续发送命令，并记录堆水位、任务栈和错误码。

命令测试不限定具体传输介质。只要目标平台实现了命令通道，就应覆盖 `STATUS`、媒体列表、播放、停止、图片、音频、亮度（若平台支持）和编码查询等已有命令。

## 9. 移植交付内容

每个目标平台至少交付：

- 平台配置文件和 BSP 适配代码；
- 适配接口说明及未实现接口列表；
- 构建命令、依赖版本和启动参数；
- 单模块、组合、异常和长稳测试记录；
- 内存/DMA 限制、线程模型和已知问题；
- 与原平台行为不一致的地方及兼容处理。

如果移植过程中改变了公共接口、命令行为、编码规则或媒体格式，必须同步更新对应头文件、测试用例和协议文档；仅替换底层实现而未改变行为时，不需要复制原平台的硬件说明。
